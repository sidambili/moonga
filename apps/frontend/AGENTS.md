Use constants from @workspace/constants package
for frontend design use .claude/skills/design-system/SKILL.md
Prefer using shadcn/ui components (install using pnpm if needed)
Never use px values for spacing or sizing.
Use tailwind classes or semantic tokens for styling
Never hardcode colors, use semantic tokens from the design system