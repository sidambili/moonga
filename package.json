{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --parallel --filter @workspace/api-server --filter @workspace/frontend run dev",
    "build": "pnpm -r --if-present run build",
    "typecheck": "tsc --build && pnpm -r --filter ./apps/** --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@esbuild/darwin-arm64": "0.27.3",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
